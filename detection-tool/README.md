A detection tool that takes input of a "bash tool call trace" of an agent and flags malicious agents.

initially - takes an input file in a decided format.

later - live monitors an actual trace produced from a running agent.